---
// The library now lives at the site root ("/"). Keep this path working for
// bookmarks and in-app links by redirecting, preserving any filter query
// (e.g. /listen?category=sermon -> /?category=sermon).
return Astro.redirect("/" + Astro.url.search, 302);
---
